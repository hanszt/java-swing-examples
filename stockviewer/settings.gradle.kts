
rootProject.name = "stockviewer"
include("stockviewer-shared")
include("stockviewer-swing")
include("stockviewer-compose")

// Since you are using the default filename (libs.versions.toml) and the default catalog name (libs), we don't actually need to register it manually in a dependencyResolutionManagement block.

